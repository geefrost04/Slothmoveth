import type { ReactNode } from 'react';
import '../course-shell.css';
import '../subject-shell.css';

export default function DailyPracticeLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
